# scango
```
Usage of ./scango:
  -config string
        location of the config file (default "./config.toml")
  -range string
        Range IP for scanning
  -redis
        Enable redis for logging
```
